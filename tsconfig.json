{
  "compilerOptions": {
    /* 기본 옵션 */
    "target": "ES2022",
    // 컴파일될 JavaScript 버전입니다.
    "module": "NodeNext",
    // 최신 Node.js의 ES 모듈 시스템 사용합니다.
    "moduleResolution": "NodeNext",
    // module 설정과 맞춰줍니다.
    "strict": true,
    // 모든 엄격한 타입 체크 활성화합니다.
    "esModuleInterop": true,
    // CommonJS 모듈을 ES 모듈 방식으로 불러올 수 있게 합니다.
    "skipLibCheck": true,
    // 설치된 라이브러리 타입 체크 스킵하여 컴파일 속도를 향상합니다.

    /* 프로젝트 구조 관련 */
    "rootDir": "./src",
    // 타입스크립트 소스코드의 루트 폴더입니다.
    "outDir": "./dist"
    // 컴파일된 JavaScript 파일이 저장될 폴더입니다.
  },
  "include": [
    "src/**/*"
  ]
  // 컴파일할 파일들의 범위를 나타냅니다.
}